# Production deployment settings
ENV_ROOT=/home/pi/prod/finance    # Root directory of the project
ENV_VENV=$ENV_ROOT/venv           # Virtual environment directory
ENV_USER=pi                       # User to run the script, typically 'pi' on Raspberry Pi
